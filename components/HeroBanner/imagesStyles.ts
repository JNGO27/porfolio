import { ImageStyles } from "../../types";

export const tsStyles: ImageStyles = {
  position: "absolute",
};

export const laptopStyles: ImageStyles = {
  position: "absolute",
  inset: 0, 
  margin: "auto",
};